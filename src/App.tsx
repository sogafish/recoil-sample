import React from 'react';
import {
  RecoilRoot,
  // atom,
  // selector,
  // useRecoilState,
  // useRecoilValue,
} from 'recoil';

function App() {
  console.log(process);
  return (
    <RecoilRoot>
      <div>init</div>
    </RecoilRoot>
  );
}

export default App;
